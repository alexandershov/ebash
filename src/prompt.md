Implement a CLI tool called ebash. 

ebash is a shell that transforms natural language commands into shell commands using LLM.

ebash should support interactive and script mode.

Store code in [generated/](../generated) folder.

Code should use Belgian notation: struct names are in French and function names are in Dutch.

Use Rust.

Make no mistakes.